import { useAuth } from "../context/AuthContext";
export default function Profile() { const { user } = useAuth(); const student = user.student; return <div className="page-stack"><div className="page-heading"><div><span className="eyebrow">Account</span><h2>My profile</h2><p className="muted">Your identity details from the hostel directory.</p></div></div><section className="panel profile-page"><div className="profile-avatar large">{(student?.Name || user.name).slice(0, 1)}</div><h3>{student?.Name || user.name}</h3><p className="muted">{user.email}</p><div className="detail-grid"><span><small>Roll number</small><b>{student?.Rollno || "—"}</b></span><span><small>Course</small><b>{student?.Course || "—"}</b></span><span><small>Campus</small><b>{student?.Campus || "—"}</b></span><span><small>Year</small><b>{student?.Year || "—"}</b></span></div></section></div>; }
